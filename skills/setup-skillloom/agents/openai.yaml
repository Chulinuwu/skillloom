interface:
  display_name: "Setup Skillloom"
  short_description: "Host or connect Skillloom and open its private Obsidian UI"
  default_prompt: "Use $setup-skillloom to configure this device across mixed-OS Tailnet setups without remote login, then verify every URL and approval."
